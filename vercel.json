{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".output",
  "public": true,
  "rewrites": [
    {
      "source": "/server/data/:file",
      "destination": "/server/data/:file"
    }
  ]
}

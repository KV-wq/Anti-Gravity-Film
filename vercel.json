{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".output",
  "public": true,
  "routes": [
    {
      "src": "/server/data/(?<file>[^/]+)",
      "dest": "/server/data/$file"
    }
  ]
}

{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": ".output",
  "public": true,
  "functions": {
    "api/**": {
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/server/data/:path*",
      "destination": "/server/data/:path*"
    }
  ]
}
